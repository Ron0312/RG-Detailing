---
import Layout from '../../layouts/Layout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import { getCollection } from 'astro:content';
import { ArrowRight, Calendar } from 'lucide-react';

const posts = await getCollection('blog');
const sortedPosts = posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Layout title="Ratgeber & News | RG Detailing" description="Tipps und Tricks zur Fahrzeugpflege, News aus der Werkstatt und Experten-Wissen von Remo Gerhardt.">
    <div class="relative min-h-[40vh] flex items-center justify-center overflow-hidden bg-black">
         <div class="absolute inset-0 bg-gradient-to-br from-zinc-900 to-black"></div>
         <div class="container mx-auto px-4 z-10 text-center pt-20">
            <span class="inline-block py-1 px-3 rounded-full bg-white/5 border border-white/10 text-zinc-300 text-sm font-bold tracking-wider mb-6 uppercase backdrop-blur-md">
                Expertenwissen
            </span>
            <h1 class="text-4xl md:text-6xl font-bold mb-6 text-white">Ratgeber & <span class="text-red-500">News</span></h1>
        </div>
    </div>

    <main class="container mx-auto px-4 py-12 -mt-20 relative z-20">
        <Breadcrumbs crumbs={[
            { label: 'Startseite', href: '/' },
            { label: 'Blog', href: '/blog' }
        ]} />

        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
            {sortedPosts.map((post) => (
                <a href={`/blog/${post.slug}`} class="glass-panel overflow-hidden group hover:bg-zinc-800 transition-colors flex flex-col h-full">
                    <div class="h-48 bg-zinc-800 relative overflow-hidden">
                        {post.data.image ? (
                            <img src={post.data.image} alt={post.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700" />
                        ) : (
                            <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent z-10"></div>
                        )}
                    </div>
                    <div class="p-8 flex flex-col flex-grow">
                        <div class="flex items-center gap-2 mb-4 text-xs font-bold text-red-500 uppercase tracking-widest">
                            <span>{post.data.category}</span>
                            <span class="w-1 h-1 rounded-full bg-zinc-600"></span>
                            <div class="flex items-center gap-1 text-zinc-500">
                                <Calendar size={12} />
                                <span>{post.data.date.toLocaleDateString('de-DE')}</span>
                            </div>
                        </div>
                        <h2 class="text-2xl font-bold text-white mb-3 group-hover:text-red-400 transition-colors">{post.data.title}</h2>
                        <p class="text-zinc-400 text-sm mb-6 line-clamp-3 leading-relaxed flex-grow">
                            {post.data.description}
                        </p>
                        <div class="mt-auto pt-4 border-t border-white/5 flex items-center text-sm font-bold text-white">
                            Artikel lesen <ArrowRight className="ml-2 w-4 h-4 transform group-hover:translate-x-1 transition-transform" />
                        </div>
                    </div>
                </a>
            ))}
        </div>

        {sortedPosts.length === 0 && (
             <div class="text-center py-20 glass-panel">
                <p class="text-zinc-500 text-lg">Noch keine Artikel vorhanden. Schauen Sie bald wieder vorbei!</p>
             </div>
        )}

        <div class="mt-20 p-12 glass-panel text-center">
            <h3 class="text-2xl font-bold text-white mb-4">Bleiben Sie auf dem Laufenden</h3>
            <p class="text-zinc-400 max-w-xl mx-auto mb-8">
                Wir arbeiten an hochwertigen Artikeln rund um die Fahrzeugpflege.
                Folgen Sie uns auf Instagram, um den Start nicht zu verpassen.
            </p>
            <a href="https://www.instagram.com/rg_detailing" target="_blank" rel="noopener noreferrer" class="inline-block bg-red-700 hover:bg-red-600 text-white px-8 py-3 rounded-full font-bold transition-colors">
                Instagram Folgen
            </a>
        </div>
    </main>
</Layout>
