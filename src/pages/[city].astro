---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import BeforeAfterSlider from '../components/BeforeAfterSlider.jsx';
import PriceCalculator from '../components/PriceCalculator.jsx';
import Breadcrumbs from '../components/Breadcrumbs.astro';

export const prerender = true;

export async function getStaticPaths() {
  const cities = await getCollection('cities');
  return cities.map(city => ({
    params: { city: city.data.slug },
    props: { cityEntry: city },
  }));
}

const { cityEntry } = Astro.props;
const { name, metaDescription, introText, geo, distance } = cityEntry.data;

const schema = {
  "@context": "https://schema.org",
  "@type": "AutoDetailing",
  "name": "RG Detailing",
  "url": `https://rg-detailing.de/${cityEntry.data.slug}`,
  "description": metaDescription,
  "areaServed": {
    "@type": "City",
    "name": name,
    "geo": {
        "@type": "GeoCoordinates",
        "latitude": geo.latitude,
        "longitude": geo.longitude
    }
  },
  "priceRange": "€€-€€€",
  "address": {
    "@type": "PostalAddress",
    "addressLocality": "Tensfeld",
    "postalCode": "23824",
    "streetAddress": "Dorfstraße 11",
    "addressCountry": "DE",
    "addressRegion": "Schleswig-Holstein"
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
      "opens": "08:00",
      "closes": "18:00"
    }
  ],
  "image": "https://rg-detailing.de/logo.png"
};
---

<Layout title={`Fahrzeugaufbereitung ${name} | RG Detailing`} description={metaDescription} schema={schema}>

    <div class="absolute top-24 left-0 w-full z-30 container mx-auto px-4">
        <Breadcrumbs crumbs={[
            { label: 'Home', href: '/' },
            { label: `Aufbereitung ${name}`, href: `/${cityEntry.data.slug}` }
        ]} />
    </div>

    {/* City Hero */}
    <div class="relative min-h-[60vh] flex items-center justify-center overflow-hidden">
        {/* Background Gradients */}
        <div class="absolute inset-0 bg-black z-0">
             <img
                src="/images/rg-detailing_Fahrzeugaufbereitung_Tensfeld_Hero.jpg"
                alt={`Premium Fahrzeugaufbereitung für Kunden aus ${name}`}
                class="w-full h-full object-cover opacity-40"
             />
        </div>
        <div class="absolute inset-0 bg-gradient-to-b from-transparent via-black/70 to-black z-10"></div>
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-red-900/20 blur-[120px] rounded-full z-0 animate-pulse"></div>

        <div class="container mx-auto px-4 z-20 text-center relative pt-20">
             <span class="inline-block py-1 px-3 rounded-full bg-white/5 border border-white/10 text-red-400 text-sm font-bold tracking-wider mb-6 uppercase">
                RG Detailing nähe {name}
            </span>
            <h1 class="text-5xl md:text-7xl font-bold mb-6 text-white tracking-tight leading-tight">
                Premium Fahrzeugaufbereitung <br/>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-red-700">für {name}</span>
            </h1>
            <p class="text-xl text-zinc-400 max-w-2xl mx-auto leading-relaxed mb-8">
                {introText}
            </p>
             <div class="flex justify-center gap-4">
                 <span class="flex items-center gap-2 text-zinc-300 bg-zinc-900/50 px-4 py-2 rounded-lg border border-white/5 backdrop-blur-sm">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-red-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                    Nur {distance} Anfahrt von {name}
                 </span>
            </div>
        </div>
    </div>

    <main class="container mx-auto px-4 py-20 relative z-20 -mt-20">

        {/* Why RG Card */}
        <div class="glass-panel p-8 md:p-12 rounded-3xl max-w-5xl mx-auto shadow-2xl mb-24 border border-white/5 bg-zinc-950/80 backdrop-blur-xl">
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div>
                    <h2 class="text-3xl font-bold mb-6 text-white">Lohnt sich der Weg aus {name}?</h2>
                    <p class="text-zinc-400 mb-6 leading-relaxed">
                        Definitiv. In {name} finden Sie vielleicht viele Waschanlagen und schnelle Aufbereiter.
                        Doch RG Detailing in Tensfeld steht für <strong>Detailversessenheit</strong> und <strong>High-End Qualität</strong>.
                        Für eine Keramikversiegelung, die 3-5 Jahre hält, lohnt sich jeder Kilometer.
                    </p>
                    <ul class="space-y-4">
                        <li class="flex items-center gap-3">
                            <div class="w-8 h-8 rounded-full bg-red-900/20 flex items-center justify-center text-red-500 shrink-0">✓</div>
                            <span class="text-zinc-300">Zertifizierter <strong class="text-white">Labocosmetica Detailer</strong></span>
                        </li>
                        <li class="flex items-center gap-3">
                             <div class="w-8 h-8 rounded-full bg-red-900/20 flex items-center justify-center text-red-500 shrink-0">✓</div>
                            <span class="text-zinc-300">Spezialist für dunkle Lacke & Leasingrückläufer</span>
                        </li>
                         <li class="flex items-center gap-3">
                             <div class="w-8 h-8 rounded-full bg-red-900/20 flex items-center justify-center text-red-500 shrink-0">✓</div>
                            <span class="text-zinc-300">Kostenloses Ersatzfahrzeug (nach Absprache)</span>
                        </li>
                    </ul>
                </div>
                <div class="bg-zinc-900/50 p-2 rounded-xl border border-white/5">
                    {/* Visual Route Placeholder */}
                    <div class="aspect-video bg-zinc-950 rounded-lg flex flex-col items-center justify-center text-zinc-600 relative overflow-hidden group">
                         <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-zinc-800/50 to-zinc-950 z-0"></div>
                         <div class="z-10 text-center">
                             <div class="text-5xl mb-4 group-hover:scale-110 transition-transform">🗺️</div>
                             <div class="text-white font-bold text-lg mb-1">{name} ➔ Tensfeld</div>
                             <div class="text-red-500 text-sm font-bold">{distance} Fahrtzeit</div>
                         </div>
                    </div>
                </div>
            </div>
        </div>

        {/* Services Grid optimized for Location */}
        <div class="mb-24">
            <div class="text-center mb-16">
                <span class="text-red-500 font-bold uppercase tracking-widest text-xs">Full Service Detailing</span>
                <h2 class="text-3xl md:text-4xl font-bold mt-4 mb-4 text-white">Unsere Expertise für {name}</h2>
                <div class="w-24 h-1 bg-red-600 mx-auto rounded-full"></div>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
                <a href="/#leistungen" class="glass-card p-8 rounded-2xl group hover:bg-zinc-900/80 cursor-pointer block border border-white/5 hover:border-red-500/30 transition-all">
                    <div class="text-4xl mb-6 group-hover:scale-110 transition-transform duration-300">✨</div>
                    <h3 class="text-xl font-bold text-white mb-3">Lackkorrektur</h3>
                    <p class="text-zinc-400 text-sm leading-relaxed">Entfernung von Kratzern, Swirls und Hologrammen. Wir holen den Neuwagen-Glanz zurück nach {name}.</p>
                </a>
                 <a href="/#leistungen" class="glass-card p-8 rounded-2xl group hover:bg-zinc-900/80 cursor-pointer block border border-white/5 hover:border-red-500/30 transition-all">
                    <div class="text-4xl mb-6 group-hover:scale-110 transition-transform duration-300">🛡️</div>
                    <h3 class="text-xl font-bold text-white mb-3">Keramikversiegelung</h3>
                    <p class="text-zinc-400 text-sm leading-relaxed">Langzeitschutz bis zu 5 Jahren. Hydrophober Effekt und extremer Tiefenglanz für Ihr Fahrzeug.</p>
                </a>
                 <a href="/#leistungen" class="glass-card p-8 rounded-2xl group hover:bg-zinc-900/80 cursor-pointer block border border-white/5 hover:border-red-500/30 transition-all">
                    <div class="text-4xl mb-6 group-hover:scale-110 transition-transform duration-300">🧖</div>
                    <h3 class="text-xl font-bold text-white mb-3">Innenraum Wellness</h3>
                    <p class="text-zinc-400 text-sm leading-relaxed">Tiefenreinigung mit 170°C Trockendampf. Hygienisch rein ohne Chemie – ideal für Familien.</p>
                </a>
            </div>
        </div>

        {/* Proof */}
        <div class="mb-24">
             <h2 class="text-3xl font-bold text-center mb-12 text-white">Ergebnisse, die überzeugen</h2>
             <div class="max-w-4xl mx-auto border border-zinc-800 rounded-2xl overflow-hidden shadow-2xl">
                 <BeforeAfterSlider
                    client:visible
                    beforeImage="https://rg-detailing.de/wp-content/uploads/2023/01/Fahrzeugaufbereitung-vorher-nacher-1.jpg"
                    afterImage="https://rg-detailing.de/wp-content/uploads/2023/01/Fahrzeugaufbereitung-vorher-nacher-2.jpg"
                    alt="Aufbereitung Vorher Nachher Vergleich"
                />
             </div>
        </div>

        {/* CTA */}
        <div id="rechner" class="glass-panel p-8 md:p-12 rounded-3xl border border-red-900/30 relative overflow-hidden">
             <div class="absolute top-0 right-0 w-64 h-64 bg-red-900/20 blur-[80px] rounded-full pointer-events-none"></div>
             <div class="relative z-10 text-center mb-12">
                 <h2 class="text-3xl font-bold mb-4 text-white">Ihr Angebot in 30 Sekunden</h2>
                 <p class="text-zinc-400">Wählen Sie Ihr Fahrzeug und erhalten Sie sofort einen Richtpreis – unverbindlich.</p>
             </div>
             <PriceCalculator client:visible />
        </div>

    </main>
</Layout>
